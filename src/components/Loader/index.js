import Spinner from "./Spiner"
import './style.css'

const Loader = () => {
    return (
        <div className="loader-container">
            <Spinner />
        </div>
    )
}

export default Loader
